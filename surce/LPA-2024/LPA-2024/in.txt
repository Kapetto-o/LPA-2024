int function testfunc1(int a, int b){
	new int res = a + b;
	return res;
};

bool function testfunc2(int chetnoe){
	new int prom = chetnoe % 5;
	new bool res = (prom != 1);
	return res;
};

main
{
	// ТИПЫ ДАННЫХ

	new int it = 1;
	new str st = "teststring";
	new bool bt = true;

	writeline "Арифметические операции:";

	new int a;
	new int b = 15;

	a = 10 * 5;
	writeline "Умножение:";
	write a;

	a = 10 / 5;
	writeline "Деление:";
	write a;

	a = 15 % 10;
	writeline "Деление с остатком:";
	write a;

	a = 10 + 5;
	writeline "Сумма:";
	write a;

	a = 10 - 5;
	writeline "Разница:";
	write a;

	a = 5+5*5;
	writeline "Приоритет (=30):";
	write a;

	a = (5+5)/5;
	writeline "Приоритет (=2):";
	write a;

	// ПОЛЬЗОВАТЕЛЬСКИЕ ФУНКЦИИ

	writeline "Пользовательские функции:";

	new int number = testfunc1(0, 5);
	new bool pravda = testfunc2(10);

	write number;
	writeline pravda;

	// БИБЛИОТЕЧНЫЕ ФУНКЦИИ

	writeline "Библиотечные функции:";

	new str stroka = "Тестовая строчка";
	new str strokachislo = "125";

	new str copystr = strduplicate(stroka, strokachislo);			// Копирование строки stroka в copystr
	new int dlina = strlength(stroka);		// Вычисление длины строки stroka
	new int strchislo = strtransint(strokachislo);	// Преобразование строки strokachislo в число

	writeline copystr;
	writeline dlina;
	writeline strchislo;

	// ОПЕРАТОР ЦИКЛА

	new int i = 0;

	cycle(i < 10)
	{
		write i;
		i = i + 1;
	}
	new int ii = 0;
	new int jj = 10;
	cycle(ii < 10)
	{
		cycle (jj > 0)
		{
			write jj;
			jj = jj - 1;
		}
		write ii;
		ii = ii + 1;
	}
};