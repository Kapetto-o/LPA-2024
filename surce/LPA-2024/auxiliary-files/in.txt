int function testfunc1(int a, int b){
	new int res = a + b;
	return res;
};

str function testfunc2(str parm1){
	new str parm2 = "TestText20";
	strduplicate(parm1, parm2);
	return parm1;
};

bool function testfunc4(int chetnoe){
	new bool phah = (chetnoe % 2 == 0);
	return phah;
};

main
{
	// ТИПЫ ДАННЫХ

	new int it = 1;
	new str st = "teststring";
	new bool bt = true;

	writeline "Арифметические операции:";

	new int a;
	new int b = 15;

	a = 10 * 5;
	writeline "Умножение:";
	write a;

	a = 10 / 5;
	writeline "Деление:";
	write a;

	a = 15 % 10;
	writeline "Деление с остатком:";
	write a;

	a = 10 + 5;
	writeline "Сумма:";
	write a;

	a = 10 - 5;
	writeline "Разница:";
	write a;

	a = 5+5*5;
	writeline "Приоритет (=30):";
	write a;

	a = (5+5)/5;
	writeline "Приоритет (=2):";
	write a;

	// ПОЛЬЗОВАТЕЛЬСКИЕ ФУНКЦИИ

	new str parm1 = "TestText1";

	new int number = testfunc1(0, 5);
	testfunc2(parm1);
	new bool pravda = testfunc4(10);

	write number;
	writeline parm1;
	writeline pravda;

	// БИБЛИОТЕЧНЫЕ ФУНКЦИИ

	new str stroka = "Тестовая строчка";
	new str strokachislo = "125";
	new str copystr;

	strduplicate(copystr, stroka);			// Копирование строки stroka в copystr
	new int dlina = strlength(stroka);		// Вычисление длины строки stroka
	new int strchislo = strtransint(strokachislo);	// Преобразование строки strokachislo в число

	writeline copystr;
	writeline dlina;
	writeline strchislo;

	// ОПЕРАТОР ЦИКЛА

	new int i = 0;

	cycle(i < 10)
	{
		write i;
		i = i + 1;
	}
};